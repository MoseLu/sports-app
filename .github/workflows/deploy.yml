name: CI/CD 后端部署

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 拉取代码
      - name: Checkout 代码
        uses: actions/checkout@v3

      # 2. 设置 Go
      - name: Setup Go 1.23.4
        uses: actions/setup-go@v4
        with:
          go-version: '1.23.4'

      # 3. 静态编译并打包
      - name: 静态编译并打包
        run: |
          cd backend
          go mod tidy
          # 关闭 cgo，静态编译为 Linux/amd64，可执行不依赖动态库
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -a -installsuffix cgo -o sports-app-linux
          tar czf ../sports-app-linux.tar.gz sports-app-linux

      # 4. 上传包到 /root/backend
      - name: 上传包到 /root/backend
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "sports-app-linux.tar.gz"
          target: "/root/backend/"

      # 5. 部署：解包→覆盖→注 .env→杀掉旧进程→后台启动
      - name: 部署并启动服务
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            set -e

            # 准备目录
            mkdir -p /root/backend

            # 解压并清理压缩包
            tar xzf /root/backend/sports-app-linux.tar.gz -C /root/backend/
            rm /root/backend/sports-app-linux.tar.gz

            # 覆盖部署目录中的二进制
            mv -f /root/backend/sports-app-linux /www/wwwroot/redamancy/backend/sports-app-linux
            chmod +x /www/wwwroot/redamancy/backend/sports-app-linux

            # 注入最新 .env（只包含 OSS 配置，如需数据库配置可一并添加）
            cat > /www/wwwroot/redamancy/backend/.env << 'EOF'
            OSS_ACCESS_KEY_ID=${{ secrets.OSS_ACCESS_KEY_ID }}
            OSS_ACCESS_KEY_SECRET=${{ secrets.OSS_ACCESS_KEY_SECRET }}
            OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
            OSS_BUCKET=sports-app-images
            EOF

            # 确保日志目录存在
            mkdir -p /www/wwwroot/redamancy/backend/logs

            # 停掉旧进程
            pkill -f sports-app-linux || true

            # 后台启动新进程，彻底脱离当前 shell
            nohup /www/wwwroot/redamancy/backend/sports-app-linux \
              > /www/wwwroot/redamancy/backend/logs/app.log 2>&1 < /dev/null &

            # 脚本成功结束
            exit 0
